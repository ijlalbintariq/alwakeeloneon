node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.aidesigner/*
!.aidesigner/.gitkeep

# Local secrets — never commit
.env
.env.*
*.env*
client/src/experimental/*.env*
.env.local

# Agent audit scripts — may contain env context, never commit
.agents/
.claude/
scratch/
composio_tools.json
*.bak*
.env.bak*
statute_docs_to_insert.json
statute_sections_to_insert.json
