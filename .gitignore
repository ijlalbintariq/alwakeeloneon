node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.aidesigner/*
!.aidesigner/.gitkeep

# Local secrets — never commit
.env
.env.local

# Agent audit scripts — may contain env context, never commit
.agents/
*.bak*
.env.bak*
