node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.aidesigner/*
!.aidesigner/.gitkeep

# Local secrets — never commit
.env
.env.*
*.env*
client/src/experimental/*.env*
.env.local

# Agent audit scripts — may contain env context, never commit
.agents/
.claude/
scratch/
composio_tools.json
*.bak*
.env.bak*
statute_docs_to_insert.json
statute_sections_to_insert.json

# Local env snapshot — contains live credentials, never commit
env-local-save.txt

# Generated by script/judgment_text_label.ts (regenerate with the dry run)
judgment_text_label_plan.csv

# Graphify tool cache
graphify-out/
server/graphify-out/

# Generated by the phase-2 citation analysis (regenerate on demand)
phase2_candidates.csv
